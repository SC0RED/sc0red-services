{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "InternalProcessThemeObjectiveTitles",
    "description": "Per-call schema for internal-processes Round 2 (per-theme objective titles). Called once per theme returned by Round 1. Each call returns 1-4 objective titles for that theme. Inner objective IDs (I{theme_idx}.{obj_idx}) are assigned by assembly.",
    "type": "object",
    "required": ["titles"],
    "additionalProperties": false,
    "properties": {
        "titles": {
            "type": "array",
            "minItems": 1,
            "maxItems": 4,
            "items": {
                "type": "string",
                "minLength": 8,
                "maxLength": 200
            }
        }
    }
}
