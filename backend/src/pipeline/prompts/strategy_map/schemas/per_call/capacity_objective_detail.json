{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CapacityObjectiveDetail",
    "description": "Per-call schema for organizational-capacity Round 2 (per-bucket elaboration). Called once per fixed bucket (people / technology / culture). The `id` (O.P/O.T/O.C) and `title` are NOT requested — assigned by assembly + provided as call input respectively. Capacity objectives have NO `category` field per the model.",
    "type": "object",
    "required": ["definition", "confidence", "rationale_source"],
    "additionalProperties": false,
    "properties": {
        "definition": {
            "type": "string",
            "minLength": 50,
            "maxLength": 1200
        },
        "confidence": {
            "type": "string",
            "enum": ["HIGH", "MEDIUM", "LOW"]
        },
        "rationale_source": {
            "type": ["string", "null"],
            "maxLength": 400
        }
    }
}
