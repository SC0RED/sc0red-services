name: 'Setup Python'
description: 'Install Python and project dependencies for Janus backend'
inputs:
  python-version:
    description: 'Python version to use'
    required: false
    default: '3.12'
  github-token:
    description: 'SSH private key or HTTPS token for accessing private GitHub dependencies (signalfield-core)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python-version }}

    - name: Configure git via SSH deploy key
      if: inputs.github-token != ''
      shell: bash
      run: |
        mkdir -p ~/.ssh
        echo "${{ inputs.github-token }}" > ~/.ssh/janus_deploy_key
        chmod 600 ~/.ssh/janus_deploy_key
        ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null
        cat >> ~/.ssh/config << 'EOF'
        Host github.com
          IdentityFile ~/.ssh/janus_deploy_key
          StrictHostKeyChecking yes
        EOF
        chmod 600 ~/.ssh/config
        git config --global url."git@github.com:".insteadOf "https://github.com/"

    - name: Install dependencies
      shell: bash
      run: |
        cd backend
        pip install -e ".[dev]"
